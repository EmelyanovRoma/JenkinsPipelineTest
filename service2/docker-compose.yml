version: '3'

services:
  service2:
    image: jerryonimo/jenkinspipelinetest-service2:latest
    ports:
      - "8001:8001"
    networks:
      - my_network

networks:
  my_network:
    external: true
